\section{Detailed Codegen Examples}
\label{app:codegen-examples}

\TODO{Worked examples:
- Het-alt: generated eBPF asm for \texttt{eth/(ipv4|ipv6)/tcp where tcp.dport == 443}, including stack layout
- Aux-header walking: bpf\_loop callback structure for \texttt{eth/ipv6/srv6 where any(srv6.segments.addr == ...)}
- Stack layout diagram (arith slots / whereLayerEntrySlot / bpf\_loop ctx)}
