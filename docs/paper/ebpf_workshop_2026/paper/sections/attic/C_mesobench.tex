\section{Mesobenchmark Details (B2)}
\label{app:mesobench}

\TODO{Per-filter cycle counts via \texttt{bpf\_prog\_test\_run}. Methodology: \texttt{pkg/kunai/dsltest/} runner, gopacket-synthesized inputs from \texttt{builders.go}, perf stat for cycle counts. Per-filter instruction breakdown (predicate vs aux-walk vs capture). Raw CSV in artifact archive.}
