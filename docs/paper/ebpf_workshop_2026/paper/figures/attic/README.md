# attic/ — 退避した図

本文 (`main.tex`) から落とした図。 削除はせず保存。

- `fig7_throughput.pdf` — end-to-end throughput (B3) の図。 B3 ベンチごと廃止した
  ため退避。 生成元は `benchmark/analysis/attic/plot_b3.py`、 入力データは
  `benchmark/results/attic/b3_throughput_ringbuf_v1.csv`。
