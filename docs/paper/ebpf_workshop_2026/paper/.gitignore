# LaTeX build output (regenerated by `make` / latexmk + bibtex from *.tex + refs.bib).
# Keep sources: *.tex, refs.bib, Makefile, figures/*.{tex,pdf}, snapshot PDFs.
*.aux
*.bbl
*.blg
*.fdb_latexmk
*.fls
*.log
*.out
*.synctex.gz
main.pdf
