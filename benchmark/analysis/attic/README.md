# attic/ — 廃止した解析スクリプト

論文から落としたベンチの plotting スクリプト。 削除はせず保存。

- `plot_b3.py` — end-to-end throughput (B3) の図 `fig7_throughput.pdf` を描く。
  B3 ベンチごと廃止したため退避 (`benchmark/results/attic/README.md` 参照)。
  入力・出力とも attic 内のパスを指すよう更新済み。 repo ルートから実行すれば
  再生成できる。
